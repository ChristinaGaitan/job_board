<h1>RailsBridgeCorp Open Jobs</h1>

<% @jobs.each do |job| %>
  <h3><%= job.title %></h3>
  <p><%= job.description %></p>
  <h5><%= link_to "Edit", edit_job_path(job)%> | <%= link_to 'Delete', job, method: :delete %></h5>
<% end %>
